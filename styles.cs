/* Reajuste global */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

/* Selector de la etiqueta body (sin el punto) */
body {
  background-color: #000000;
  color: #ffffff; /* Añadido para que el texto sea visible sobre fondo negro */
  font-family: system-ui, -apple-system, sans-serif;
}
